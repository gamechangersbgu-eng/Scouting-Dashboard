# Copy these values into Render's Environment settings. Never commit real values.
APP_ENV=production
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/postgres
FLASK_SECRET_KEY=replace-with-a-long-random-secret
